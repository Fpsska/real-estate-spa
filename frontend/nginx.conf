server {
    listen 8080;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback: unknown paths are served index.html so client-side routing works
    location / {
        try_files $uri $uri/ /index.html;
    }
}
