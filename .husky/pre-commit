# Hook run eslint checks when creating a commit

fallbackMessage="
========================================================================
🤯 Your code did not pass the lint check 🤯 Fix problems and try again!
========================================================================
"

npm run lint --prefix frontend && npm run lint --prefix backend || (echo "$fallbackMessage" && exit 1)
